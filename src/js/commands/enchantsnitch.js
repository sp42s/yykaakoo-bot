import { logger } from '../lib/logger'

let findAllMissingEnchants = () => {
    return new Promise((resolve, reject) => {
        console.log(`lumoukset`)
        if (true) resolve('kellää ei oo lumouksia' )
        else reject('Mitehä tää tulis')
    })
}
    







export { findAllMissingEnchants }